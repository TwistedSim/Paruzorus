version: '3.7'

services:
  
  frontend:
    build:
      target: build-stage
    volumes:
      - /app/node_modules
      - ./frontend:/app
    command: ["yarn", "start"]

  webserver:
    volumes:
      - ./webserver:/app
    environment:
      - CONFIGURATION_SETUP=dev
      - FLASK_ENV=development
